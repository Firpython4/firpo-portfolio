import Head from "next/head";

import background from 'public/templates/landing-template.png'

const HeroTitleCard = (props: {className?: string}) =>
{
    return <div className={`flex flex-col ${props.className}`}>
        <h3 className="text-slate-100 center text-5xl">
            Marcelo<br/>Firpo
        </h3>
        <p className="text-slate-100">
            CMO, Diretor de<br/>Criação, Consultor<br/>Criativo, Redator,<br/>Roteirista
        </p>
    </div>;
};

const BackgroundImageStyle =(image: {src: string}) =>
{
    return {backgroundImage: `url(${image.src})`}
}

export default function Home() {
  return (
    <>
        <Head>
            <title>Marcelo Firpo</title>
            <meta name="description" content="Generated by create-t3-app"/>
            <link rel="icon" href="/favicon.ico"/>
        </Head>
        <main className="flex flex-col items-center justify-center bg-slate-200 overflow-hidden">
            <div className={`h-full w-full bg-no-repeat bg-center bg-cover pb-12`} style={{backgroundImage: `url(${background.src})`}}>
                <div className="flex flex-row justify-center pt-80">
                    <HeroTitleCard className="pl-20"/>
                </div>
            </div>
        </main>
    </>
  );
}
